HOME_PATH="${HOME}/.bin:${HOME}/.local/bin"
BREW_PATH="/opt/homebrew/bin:/opt/homebrew/opt/node@20/bin"
export PATH="${PATH}:${BREW_PATH}:${HOME_PATH}"

if [ -x /usr/libexec/path_helper ]; then
   eval `/usr/libexec/path_helper -s`
fi

